#!/bin/sh

indent() {
  sed -u 's/^/       /'
}

BUILD_DIR=$1
CACHE_DIR=$2

if ! [ -f $CACHE_DIR/elm ]; then
  echo "-----> Downloading Elm $ELM_VERSION"
  curl -L -o $CACHE_DIR/elm.gz https://github.com/elm/compiler/releases/download/$ELM_VERSION/binary-for-linux-64-bit.gz
  gunzip $CACHE_DIR/elm.gz
fi

echo "-----> Installing Elm $ELM_VERSION"
cp $CACHE_DIR/elm $BUILD_DIR/bin
chmod +x $BUILD_DIR/elm

echo elm --version
echo "Path is $PATH"
